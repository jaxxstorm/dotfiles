# Path to your oh-my-zsh installation.
export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="robbyrussell"
zstyle ':omz:update' mode reminder  # just remind me to update when it's time

COMPLETION_WAITING_DOTS="true"

source $ZSH/oh-my-zsh.sh

plugins=(git ssh-agent aws)

export PATH="$HOME/.cargo/bin:$HOME/.tfenv/bin:$HOME/.gobrew/current/bin:$HOME/.gobrew/bin:/opt/homebrew/bin:/opt/homebrew/lib/ruby/gems/3.1.0/bin:/opt/homebrew/opt/ruby/bin:$PATH"

# Starship
eval "$(starship init zsh)"

# awsp
alias awsp="source _awsp"

# GPG
# Add the following to your shell init to set up gpg-agent automatically for every shell
export GPG_TTY=$(tty)
gpgconf --launch gpg-agent

export KUBECONFIG=KUBECONFIG:$(echo $(ls ~/.kube/config.d/*) | sed 's/ /:/g')
export EDITOR=vim
eval "$(/Users/lbriggs/.local/bin/mise activate zsh)"
